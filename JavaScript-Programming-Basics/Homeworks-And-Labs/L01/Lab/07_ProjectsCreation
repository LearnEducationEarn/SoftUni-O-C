function projectsCreation(input) {
    const HOURS_PER_PROJECT = 3;
    
    const name = input[0];
    const countProjects = Number(input[1]);

    const result = HOURS_PER_PROJECT * countProjects;

    console.log(`The architect ${name} will need ${result} hours to complete ${countProjects} project/s.`);






}
